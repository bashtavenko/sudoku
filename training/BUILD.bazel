load("@rules_cc//cc:defs.bzl", "cc_binary", "cc_library", "cc_test")

cc_binary(
    name = "train_main",
    srcs = ["train_main.cc"],
    deps = [
        "//:digit_detector",
        "@absl//absl/flags:flag",
        "@absl//absl/flags:parse",
        "@gflags",
        "@glog",
    ],
)
